name: qr_order_app
description: "A QR Order App project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  firebase_core: ^4.3.0
  cloud_firestore: ^6.1.1
  flutter_web_plugins:
    sdk: flutter
  audioplayers: ^5.2.1
  image_picker: ^1.0.4
  http: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # Khai báo thư mục ảnh
  assets:
    - assets/images/
    - assets/sound/